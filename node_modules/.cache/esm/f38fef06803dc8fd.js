let Product;_803‍.x([["postAddProduct",()=>postAddProduct],["getAllProducts",()=>getAllProducts],["getProductById",()=>getProductById],["postEditProduct",()=>postEditProduct],["postDeleteProduct",()=>postDeleteProduct]]);_803‍.w("../models/product",[["Product",["Product"],function(v){Product=v}]]);

// create
       const postAddProduct = (req, res, next) => {
  const product = new Product({
    title: req.body.title,
    price: req.body.price,
    description: req.body.description,
    imageUrl: req.body.imageUrl,
  })
  product
    .save()
    .then(result => {
      console.log('Created Product')
      res.send('Created Product! Check your DB')
      // res.redirect('/admin/products')
    })
    .catch(err => _803‍.g.console.log(err))
}

// read all
        const getAllProducts = (req, res, next) => {
  Product.find()
    .then(products => {
      res.json(products)
    })
    .catch(err => _803‍.g.console.log(err))
}


// read one
       const getProductById = (req, res, next) => {
  const prodId = req.body.productId
  _803‍.g.console.log(prodId)
  Product.findById(prodId)
    .then(product => {
      if (!product) {
        return res.redirect('/')
      }
      res.json(product)
    })
    .catch(err => _803‍.g.console.log(err))
}

// update
       const postEditProduct = (req, res, next) => {
  const prodId = req.body.productId
  const updatedTitle = req.body.title
  const updatedPrice = req.body.price
  const updatedDesc = req.body.description
  const updatedImageUrl = req.body.imageUrl
  
  Product.findById(prodId)
  .then(product => {
    product.title = updatedTitle
    product.price = updatedPrice
    product.description = updatedDesc
    product.imageUrl = updatedImageUrl
    return product.save()
  })
    .then(result => {
      console.log('Updated product')
      res.redirect('/admin/getAllProducts')
    })
    .catch(err => _803‍.g.console.log(err))
}

// delete

       const postDeleteProduct = ( req, res, next) => {
    const prodId = req.body.productId
    Product.findByIdAndRemove(prodId)
    .then(() => {
        console.log('Deleted the product')
        res.redirect('/admin/getAllProducts')
    })
    .catch(err => _803‍.g.console.log(err))
} 
